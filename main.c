#include <stdio.h>
#include <stdbool.h>


int main() {

    // int x = 15;
    // int y = 6;
    // int max = (x >= y) ? x : y;

    // bool isOnline = true;
    // printf("%s", (isOnline) ? "he is online" : "he is offline");

    
    return 0;

}